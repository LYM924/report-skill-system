---
name: keyword-library
description: 产品智能问答入口。用于回答用户产品咨询、问题排查、知识定位。触发：用户咨询产品功能、询问系统用法、反馈问题、搜索业务知识时。
---

# 智能问答入口

## 文件结构

```
关键词库/
├── SKILL.md              ← 本文件（问答编排 + 回答策略）
├── 关键词索引.md           ← 关键词 → 模块映射表（数据源）
├── synonyms.json          ← 同义词/别名/拼音映射
├── index_cache.json       ← 索引缓存（自动生成，加速检索）
└── faq_cache.json         ← FAQ 回答缓存

智能检索工具/              ← 检索引擎代码（独立目录）
├── search_engine.py       ← 检索引擎（分词+扩展+多源搜索+排序）
├── search_server.py       ← Web 搜索服务
├── bm25_index.py          ← BM25 索引
├── vector_index.py        ← 向量语义索引
├── requirements.txt
└── static/index.html      ← Web 搜索界面
```

## 问答工作流

当用户提出产品咨询或问题时，按以下流程处理：

```
用户提问
  │
  ├── 0. 查 FAQ 缓存（新增）
  │     faq_cache.json 中匹配相似问题
  │     → 命中则直接返回缓存回答，秒出
  │
  ├── 1. 运行检索引擎
  │     python3 智能检索工具/search_engine.py "用户问题"
  │     → 获取结构化检索结果（模块定位 + 知识库匹配 + 历史工单）
  │
  ├── 2. 双路回答生成
  │     ├── 规则引擎回答：秒出，基于关键词匹配+模板拼装
  │     └── Claude 深度分析：后端自动调用 Anthropic API，SSE 流式推送
  │
  ├── 3. 反哺优化（自动）
  │     Claude 回答完成后：
  │     ├── 保存到 FAQ 知识库（2026产品业务知识库/{部门}/{业务域}/FAQ.md）
  │     ├── 提取新关键词，补充到关键词索引
  │     └── 写入 faq_cache.json 缓存
  │
  └── 4. 生成人性化回答
        按回答模板组织回复（见下方）
```

## 回答模板

根据问题类型选择合适模板，以**自然、友好、专业**的语气回复用户：

### 模板 A：功能咨询（"XXX怎么用"、"XXX在哪里"）

```
💬 关于「{功能名}」：

📍 所属模块：{模块名}
   ├── 产品线：{产品} / {产品线}
   ├── 菜单路径：{一级菜单} → {二级菜单} → {三级菜单}
   └── 负责人：研发 {dev_owner} / 模块 {module_owner}

📋 功能说明：
   {从知识库文档中提取的功能描述}

💡 使用提示：
   {如果有相关注意事项、配置项等}

🔗 参考文档：{知识库文档路径}
```

### 模板 B：问题排查（"XXX报错"、"XXX不能用"）

```
💬 关于「{问题描述}」：

📍 这个问题属于「{模块名}」模块，由 {module_owner} 负责。

🔍 常见原因：
   1. {原因1}
   2. {原因2}

🔧 建议排查步骤：
   1. {步骤1}
   2. {步骤2}

📊 历史类似问题：
   {从报表数据中提取的历史工单}

💡 如果以上步骤无法解决，建议：
   - 联系模块负责人 {module_owner}（研发 {dev_owner}）
   - 提交技术支持工单，归类到「{模块名}」→ 附录{appendix}
```

### 模板 C：模块/负责人查询（"XXX谁负责"）

```
💬 「{模块名}」模块信息：

   ├── 所属部门：{dept}
   ├── 业务域：{domain}
   ├── 产品线：{product} / {product_line}
   ├── 研发负责人：{dev_owner}
   ├── 模块负责人：{module_owner}
   ├── 周报附录：{appendix}
   ├── Skill 文件：{path}
   └── 知识库：{kb_path}
```

### 模板 D：模糊匹配（检索结果不明确时）

```
💬 关于「{用户问题}」：

我找到了以下可能相关的模块，请问你指的是哪个？

1. {模块1} — {一句话说明}
2. {模块2} — {一句话说明}
3. {模块3} — {一句话说明}

你也可以提供更多细节，我帮你进一步定位。
```

## 检索引擎使用

```bash
# 基础搜索
python3 智能检索工具/search_engine.py "用户问题"

# 指定返回数量
python3 智能检索工具/search_engine.py "用户问题" --top 5

# 重建索引（新增模块/知识库文档后）
python3 智能检索工具/search_engine.py "用户问题" --rebuild

# 输出原始 JSON（供程序调用）
python3 智能检索工具/search_engine.py "用户问题" --json
```

## 数据维护

### 新增同义词

编辑 `synonyms.json`，在对应分类下追加：

```json
"新词": ["同义词1", "同义词2", "别名3"]
```

### 新增关键词

1. 在 `关键词索引.md` 表格中追加行
2. 同步更新对应模块 Skill 文件的 `## 关键词` 和 frontmatter
3. 运行 `python3 search_engine.py "test" --rebuild` 重建索引

### 新增知识库文档

将新文档放入 `2026产品业务知识库/` 对应目录后，运行 `--rebuild` 重建索引即可自动纳入检索范围。

### 修改关键词

1. 在 `关键词索引.md` 中找到对应行，修改关键词
2. 同步更新对应模块 Skill 文件的 `## 关键词` 章节和 frontmatter `description`
3. 运行 `--rebuild` 重建索引

### 删除关键词

1. 从 `关键词索引.md` 中删除对应行
2. 从模块 Skill 文件的 `## 关键词` 章节中移除
3. 更新模块 Skill 文件 frontmatter `description`
4. 运行 `--rebuild` 重建索引

### 维护原则

- 关键词索引是**主数据源**，模块 Skill 文件中的关键词为**副本**
- 修改时以关键词索引为准，同步更新模块文件
- 关键词使用中文逗号分隔
- 同义词/别名统一在 `synonyms.json` 中维护

## 双向链接

关键词库已打通与其他三层的双向链接：

- 📁 **模块文件**：关键词索引每行新增"模块文件"列，可直接跳转到对应模块文件
- 📚 **知识库**：关键词索引每行有"知识库路径"列，指向版本迭代文档
- 📊 **报表**：关键词索引底部有"反向索引"章节，从报表定位到关键词和模块
- 🗺️ **中央枢纽**：[双向链接枢纽.md](../双向链接枢纽.md) — 四层体系总导航

### 新增模块时的链接维护

1. 在 `关键词索引.md` 中添加关键词行，填写"模块文件"列
2. 在模块文件的 `## 双向链接` 章节中确保有回链到关键词索引
3. 在 `双向链接枢纽.md` 中添加对应行
4. 运行 `--rebuild` 重建索引

## 双路回答与反哺优化

### 双路回答机制

搜索工具同时提供两种回答：

| 通道 | 来源 | 速度 | 质量 | 展示 |
|------|------|------|------|------|
| 规则检索 | 关键词匹配 + 段落提取 | 秒出 | 中等 | 🔍 AI 智能回答卡片 |
| Claude 深度分析 | Anthropic API 流式调用 | 3-10s | 高 | 🧠 Claude 深度分析卡片 |

### 反哺优化（自动）

Claude 回答完成后，系统自动执行以下优化：

1. **FAQ 知识库沉淀** — 保存 Q&A 到 `2026产品业务知识库/{部门}/{业务域}/FAQ.md`
2. **关键词补充** — 提取 Claude 建议的新关键词，补入 `关键词索引.md`
3. **回答缓存** — 写入 `faq_cache.json`，下次同类问题直接命中，无需再调 API

### FAQ 缓存命中

用户搜索时，系统优先检查 `faq_cache.json`。如果命中（关键词交集 ≥ 2），直接返回缓存的 Claude 回答，速度与规则引擎相当，质量等同 Claude。

### 配置要求

- **ANTHROPIC_API_KEY** — 环境变量，与 Claude Code 共用。未配置时工具照常运作，仅不显示 Claude 卡片
- **CLAUDE_MODEL** — 可选，默认 `claude-sonnet-4-20250514`